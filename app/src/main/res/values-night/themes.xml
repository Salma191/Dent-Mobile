<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DentaireProjet" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/purple</item>
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
    <style name="CustomTheme" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/purple</item>
        <item name="toolbarStyle">@style/ToolbarTheme</item>
    </style>
    <style name="ThemeOverlay.App.Toolbar.Dark." parent="ThemeOverlay.App.Toolbar.Dark">
        <item name="colorOnPrimary">@color/white</item>
    </style>
    <style name="SmoothCheckBoxStyle.">
        <item name="color_checked">@color/purple</item>
        <item name="color_tick">@color/white</item>
    </style>
</resources>